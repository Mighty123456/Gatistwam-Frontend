export const API_URL = 'https://gatistwam-backend.onrender.com';

// API endpoints
export const API_ENDPOINTS = {
  // Auth endpoints
  LOGIN: `${API_URL}/api/users/login`,
  REGISTER: `${API_URL}/api/users/register`,
  
  // Blog endpoints
  BLOG: `${API_URL}/api/blog`,
  
  // Portfolio endpoints
  PORTFOLIO: `${API_URL}/api/portfolio`,
  
  // Contact endpoints
  CONTACT: `${API_URL}/api/contact`,
  
  // Admin endpoints
  ADMIN: `${API_URL}/api/admin`,
}; 